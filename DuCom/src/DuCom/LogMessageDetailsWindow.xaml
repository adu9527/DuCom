<Window x:Class="DuCom.LogMessageDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="{DynamicResource LogAnalyzer.MessageDetails}" Width="680" Height="330" MinWidth="480" MinHeight="240"
        Icon="/Assets/DuCom.ico" WindowStartupLocation="CenterOwner" ResizeMode="CanResize" ShowInTaskbar="False"
        Background="{DynamicResource Brush.ShellSurface}">
    <Grid Background="{DynamicResource Brush.ShellSurface}">
        <Grid.RowDefinitions><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
        <TextBox x:Name="MessageBox" Margin="14" Padding="10" IsReadOnly="True" AcceptsReturn="True"
                 TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" />
        <StackPanel Grid.Row="1" Margin="14,0,14,14" HorizontalAlignment="Right" Orientation="Horizontal">
            <Button MinWidth="96" Margin="0,0,8,0" Click="CopyAll_Click" Content="{DynamicResource LogAnalyzer.CopyAll}" />
            <ui:Button MinWidth="88" Appearance="Primary" Click="Close_Click" Content="{DynamicResource Common.Close}" />
        </StackPanel>
    </Grid>
</Window>
